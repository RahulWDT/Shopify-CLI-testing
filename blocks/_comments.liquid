{% assign count = article.comments_count %}
<span class="comment">
  {% if count == 1 -%}
    {{- 'blogs.article.comments.one' | t: count: count }}
  {%- endif %}
</span>

{% schema %}
{
  "name": "t:name.comments",
  "tag":null,
  "settings": []
}
{% endschema %}
