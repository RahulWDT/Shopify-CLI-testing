{% liquid
  if template == 'search'
    assign result = search.filters    
    assign terms = search.terms | escape
    assign results_url = '?q=' | append: terms | append: '&options%5Bprefix%5D=last&sort_by=' | append: sort_by    
    else
    assign result = collection.filters  
    assign results_url = result.url  
  endif  
%}
 <div class="collection_filter {% unless show_active_filters %} d-none{% endunless %}">
    <div
      id="selected-filters"
      class="filter-tags-container mb-3 mt-2"
    >
      {%- for filter in result -%}
        {%- if filter.type == 'price_range' -%}
          {%- if filter.min_value.value != null or filter.max_value.value != null -%}
            <div class="filter-tag position-relative">
              <a href="{{ filter.url_to_remove }}" class="btn-close" style="position:absolute; top:-6px; right:-6px;">
                {% render 'cancel.svg' %}
              </a>
              <span>
                {%- assign min_value = filter.min_value.value | default: 0 -%}
                {%- assign max_value = filter.max_value.value | default: filter.range_max -%}
                {{ min_value | money }} - {{ max_value | money }}
              </span>
            </div>
          {%- endif -%}
        {%- else -%}
          {%- for filter_value in filter.active_values -%}
            <div class="filter-tag position-relative">
              <a
                href="{{ filter_value.url_to_remove }}"
                class="btn-close"
                style="position:absolute; top:-6px; right:-6px;"
              >
                {% render 'cancel.svg' %}
              </a>
              <span> {{ filter_value.label }}</span>
            </div>
          {%- endfor -%}
        {%- endif -%}
      {%- endfor -%}
    </div>  
     <button id="clear-all-filters" data-path="{{ results_url }}" class="btn clear-all-filters  btn-link clear_filter text-decoration-underline mt-2 mb-3 text-reset" hidden>
      {{ 'products.facets.remove_all' | t }}
     </button>
  </div>